# MySpace — خطة المشروع (Project Plan)

**المستخدم:** Abdulmalik — استخدام شخصي لتنظيم التاسكات في هواوي
**التاريخ:** 2026-08-18

---

## 1) الفكرة بإيجاز

تطبيق ويب شخصي اسمه **MySpace** يساعدك تتابع التاسكات الجاية لك من أشخاص مختلفين في الشغل. لكل تاسك كارد فيه من طلبه، أولويته، وحالته. مبني بحيث تقدر تضيف عليه ميزات جديدة بسهولة مستقبلاً، ومنشور بشكل تقدر توصله من لابتوب الشغل عبر الإنترنت.

---

## 2) ميزات النسخة الأولى (MVP)

### إدارة التاسكات
- إنشاء/تعديل/حذف كارد تاسك.
- كل كارد فيه:
  - العنوان (title)
  - الوصف/التفاصيل (notes)
  - **الشخص الطالب** (requester) — اسم + ممكن قسمه/فريقه
  - **الأولوية**: عالية / متوسطة / منخفضة
  - **الحالة**: جديد / قيد التنفيذ / متوقف / مكتمل
  - تاريخ الاستحقاق (due date)
  - تاريخ الإنشاء وآخر تحديث (تلقائي)

### العرض والتنظيم
- عرض لوحة (Board / Kanban) مقسم حسب الحالة: جديد → قيد التنفيذ → مكتمل.
- فلترة وترتيب حسب: الشخص الطالب، الأولوية، تاريخ الاستحقاق.
- تمييز التاسكات المتأخرة (overdue) بلون مختلف.
- صفحة رئيسية (Dashboard) فيها ملخص سريع: عدد التاسكات المفتوحة، كم منها عالي الأولوية، كم متأخر.

### الحسابات (Auth)
- تسجيل دخول/تسجيل حساب كامل (Email + Password).
- كل بياناتك محمية خلف حسابك — مهم لأنك بتفتحه من لابتوب الشغل.

---

## 3) نموذج البيانات (Data Model)

**Task** (جدول وحيد — `public.tasks` بـ Supabase)
`id, title, description, requester_name (نص حر، اختياري), priority [High/Medium/Low], status [New/InProgress/Blocked/Done], due_date, user_id, created_at, updated_at`

> الحسابات نفسها تدار بالكامل من Supabase Auth (`auth.users`) — ما نحتاج جدول users خاص فينا. و"الشخص الطالب" حقل نص حر داخل التاسك مباشرة، مو جدول منفصل.

---

## 4) التقنية المستخدمة (Tech Stack)

> **ملاحظة:** الستاك تغيّر عن الخطة الأصلية (كانت Next.js/Prisma/Turso/NextAuth) عشان يطابق مشروع "kalemati" السابق — خفيف وسريع بدون باك اند منفصل.

| الجزء | الاختيار | السبب |
|---|---|---|
| Framework | **React 19 + Vite 7 + TypeScript** | خفيف وسريع، بدون تعقيد سيرفر |
| التصميم | **Tailwind CSS** | تصميم سريع ونظيف بدون ما تكتب CSS كثير |
| الأيقونات | **lucide-react** | |
| قاعدة البيانات | **Supabase (Postgres + Row Level Security)** | كل مستخدم يشوف بياناته بس، بدون كتابة أي كود تحقق يدوي |
| نظام الحسابات | **Supabase Auth** | تسجيل دخول/حساب بالإيميل جاهز، مربوط مباشرة بالـ RLS |
| الـ Backend | **بدون — CRUD مباشر من الفرونت عبر Supabase SDK** | RLS يحمي البيانات، ما نحتاج API منفصل بالمرحلة الأولى. لو احتجنا منطق سيرفر لاحقاً (مثلاً ميزات AI) نضيف `api/` كـ Vercel Serverless Functions وقتها بس |
| الاستضافة | **Vercel** (مجاني للاستخدام الشخصي) | نشر بضغطة، رابط تفتحه من أي مكان بما فيه لابتوب الشغل |

---

## 5) خارطة الطريق المستقبلية (Roadmap)

**v1 — الأساس (المذكور أعلاه)**
تراك التاسكات + كروت + أولويات + حسابات + نشر أونلاين.

**v2 — تحسينات الإنتاجية**
- تذكيرات/إشعارات للتاسكات القريبة من الاستحقاق.
- تعليقات/سجل نشاط على كل تاسك.
- أرشفة التاسكات المكتملة بدل حذفها.
- بحث سريع.
- تقرير أسبوعي بسيط (كم تاسك خلصت، كم متبقي).

**v3 — تكاملات**
- ربط مع Outlook/Teams (شائعة بهواوي) لسحب المهام أو التنبيه فيها.
- نسخة PWA تشتغل مثل تطبيق موبايل.
- دعم أكثر من مستخدم/صلاحيات (لو حبيت تشارك فريقك لاحقاً).

**v4 — ذكاء اصطناعي**
- تلخيص تلقائي للتاسكات الطويلة.
- اقتراح أولوية تلقائي حسب الكلام المكتوب في التاسك.
- ربط بتقويم Outlook/Google.
